{
  "name": "@mono/app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "set PORT=3011 && nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.10"
  }
}
